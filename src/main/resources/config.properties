# C?u hình WebDriver
webdriver.chrome.driver=src/test/resources/drivers/chromedriver.exe

# URL c? s? c?a ?ng d?ng
base.url=http://localhost:5500

# Timeouts (tính b?ng giây)
implicit.wait=10
page.load.timeout=30
script.timeout=30

# Môi tr??ng test
test.env=local

# Báo cáo
enable.screenshots=true
report.output.dir=test-output/reports